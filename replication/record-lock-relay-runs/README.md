# Raw bench output for RECORD_LOCK_RELAY_TRANSPORT.md

One file per run of `npm run bench:record-lock-relay`, written by the bench to
`RECORD_LOCK_RELAY_BENCH_OUT`. Measured on harper-pro#865's branch, machine class as stated in the
results document.

| file                   | command                                                         |
| ---------------------- | --------------------------------------------------------------- |
| `transport-run-1.json` | `RECORD_LOCK_RELAY_BENCH_OUT=… npm run bench:record-lock-relay` |
| `transport-run-2.json` | as above                                                        |
| `transport-run-3.json` | as above                                                        |

All three are the bench as committed, at its default 3 000 samples per row. The document's table
reports run 1; the spread claim is over all three.
